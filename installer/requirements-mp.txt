# Pinned runtime deps for faceswap-mp on Windows (Python 3.11 + RTX GPU).
# Smaller than the full requirements-webapp.txt — only what webapp_mp.py needs.
# install.ps1 pip-installs these into the `dlc` conda env.

# Core ML
numpy>=1.23.5,<2
opencv-python==4.10.0.84
onnx==1.18.0
onnxruntime-gpu==1.23.2
insightface==0.7.3
protobuf==4.25.1
pillow==12.1.1

# CUDA runtime DLLs so onnxruntime-gpu finds cuDNN 9 + CUDA 12 on Windows.
# These pip wheels drop the DLLs at site-packages/nvidia/*/bin/ and we
# register those dirs via os.add_dll_directory() at process start.
nvidia-cudnn-cu12
nvidia-cublas-cu12
nvidia-cuda-runtime-cu12
nvidia-cuda-nvrtc-cu12
nvidia-curand-cu12
nvidia-cufft-cu12
nvidia-nvjitlink-cu12

# Web app
flask>=3.0
